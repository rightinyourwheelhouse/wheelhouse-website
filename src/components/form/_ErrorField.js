import styled from 'styled-components';

export default styled.span`
  display: block;
  font-size: 17px;
  color: var(--color-error);
  letter-spacing: 0;
  font-weight: bold;
  line-height: 22px;
  margin-top: 5px;
`;
