import styled from 'styled-components';

import colors from '../../styles/colors';

export default styled.span`
  display: block;
  font-size: 17px;
  color: ${colors.error};
  letter-spacing: 0;
  font-weight: bold;
  line-height: 22px;
  margin-top: 5px;
`;
